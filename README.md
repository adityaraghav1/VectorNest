# VectorNest

VectorNest is a production-inspired vector database built from scratch in Python.

The project implements vector storage, similarity search, metadata filtering,
persistence, Brute Force, KD-Tree, and an educational HNSW index without using
vector-database libraries.
